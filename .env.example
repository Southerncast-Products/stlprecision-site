# Local development only. On Netlify these live in
# Site configuration -> Environment variables.

# Password for the /admin quote dashboard
ADMIN_PASSWORD=change-me

# Netlify personal access token (User settings -> Applications -> Personal access tokens).
# Used by the quotes function to read form submissions.
NETLIFY_API_TOKEN=
